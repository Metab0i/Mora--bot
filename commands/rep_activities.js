const Discord = require('discord.js');
const assist_func = require('./assist_functions');
const rep_fund = require('./reputation.js');

module.exports = {
    //TODO
    /**
     * implement ways for users to:
     *      make more xp
     *      buy roles
     *      buy other privileges temporarily(?)
     */
}